<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home_fragment">

    <fragment
        android:id="@+id/nav_home_fragment"
        android:name="com.example.vaicheuserapp.ui.dashboard.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_profile_fragment"
        android:name="com.example.vaicheuserapp.ui.profile.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />

    <!-- Notification List Fragment (used by ViewPager, not directly navigated to by bottom nav) -->

    <!-- Notification Detail Fragment -->
    <fragment
        android:id="@+id/nav_notifications_fragment"
        android:name="com.example.vaicheuserapp.ui.notifications.NotificationsFragment"
        android:label="Notifications"
        tools:layout="@layout/fragment_notifications">
        <!-- The action to NotificationDetailFragment is REMOVED -->
    </fragment>

    <!-- Notification List Fragment (used by ViewPager, not directly navigated to by bottom nav) -->
    <fragment
        android:id="@+id/notificationListFragment"
        android:name="com.example.vaicheuserapp.ui.notifications.NotificationListFragment"
        android:label="NotificationListFragment"
        tools:layout="@layout/fragment_notification_list" />

    <fragment
        android:id="@+id/nav_history_fragment"
        android:name="com.example.vaicheuserapp.ui.history.HistoryFragment"
        android:label="History"
        tools:layout="@layout/fragment_history" />

    <!-- NEW: Destination for the Sell flow -->
    <fragment
        android:id="@+id/set_plan_fragment"
        android:name="com.example.vaicheuserapp.ui.sell.SetPlanFragment"
        android:label="Set Plan"
        tools:layout="@layout/fragment_set_plan" />

</navigation>