<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragment.DashboardFragment">

    <!-- Google Map Placeholder -->
    <!-- Đây có thể là một FragmentContainerView để chứa Google Map thật -->
    <FrameLayout
        android:id="@+id/map_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:background="#E0E0E0">
        <!-- Google Map Fragment sẽ được thêm vào đây từ code -->
    </FrameLayout>


    <!-- Top bar: Earnings - Online/Offline - Notifications -->
    <LinearLayout
        android:id="@+id/top_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="48dp"
        android:layout_marginHorizontal="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Left Icon -->
        <FrameLayout
            android:id="@+id/btnLeftContainer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/bg_top_button"
            android:elevation="4dp"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackgroundBorderless">
            <ImageView
                android:id="@+id/btnLeft"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_earn"
                android:contentDescription="Earnings" />
        </FrameLayout>

        <!-- Center Text -->
        <TextView
            android:id="@+id/tvCenterTitle"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="16dp"
            android:background="@drawable/bg_top_button"
            android:elevation="4dp"
            android:gravity="center"
            android:text="Offline"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"/>

        <!-- Right Icon -->
        <FrameLayout
            android:id="@+id/btnRightContainer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/bg_top_button"
            android:elevation="4dp"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackgroundBorderless">
            <ImageView
                android:id="@+id/btnRight"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:contentDescription="Notifications"
                android:src="@drawable/ic_notifications" />
        </FrameLayout>

    </LinearLayout>


    <!-- Bottom Navigation -->
    <include
        android:id="@+id/bottom_nav_include"
        layout="@layout/include_bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true" />

</RelativeLayout>